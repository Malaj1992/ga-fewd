console.log("WRITE YOUR SOLUTION IN HERE!");

var num=0;

var num = 0;
    while (num < 4) {
      d.move(num, 1);
      num = num + 1;

}

d.orient("down")
var num = 5;
    while (num <= 20) {
      d.move(num, 1);
      num = num + 1;
  }

  d.orient("right")
var num = 20;
    while (num <= 25) {
      d.move(num, 1);
      num = num + 1;
  }


  d.orient("up")
var num = 25;
    while (num <= 35) {
      d.move(num, 1);
      num = num + 1;
  }

  d.orient("right")
var num = 35;
    while (num <= 40) {
      d.move(num, 1);
      num = num + 1;
  }

 d.orient("down")
var num = 40;
    while (num <= 48) {
      d.move(num, 1);
      num = num + 1;
  }

   d.orient("right")
var num = 48;
    while (num <= 53) {
      d.move(num, 1);
      num = num + 1;
  }


  


  
